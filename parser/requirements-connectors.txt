# Optional dependencies for remote data-source connectors.
# Install only the SDKs for the sources you use, e.g. `pip install boto3`.
# The core parser install (requirements.txt) does not need these; each
# connector imports its SDK lazily and reports a clear error if it's missing.
boto3>=1.34.0                    # s3 connector
google-cloud-storage>=2.16.0     # gcs connector
azure-storage-blob>=12.19.0      # azure_blob connector
google-api-python-client>=2.100.0  # gdrive connector (+ google-auth)
